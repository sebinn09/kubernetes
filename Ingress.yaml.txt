apiVersion:extension/v1beta1
kind: Ingress
metadata:
 labels:
  app:linuxacademy
 name:prod-linuxacademy
 namespace:prod
spec:
 rules:
  -host:prod-linuxacademy.com
   http:
    paths:
     -backend:
       serviceName:prod-sample01
       servicePort:8080
      path:/
     -backend:
       serviceName:prod-sample02
       servicePort:8080
      path:/actuator/health
